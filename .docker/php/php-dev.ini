; Development PHP overrides.
; Replaces php-prod.ini in the development Docker build target.

[PHP]
display_errors         = On
display_startup_errors = On
error_reporting        = E_ALL

[opcache]
; Re-check every request so code changes are visible without restart.
opcache.validate_timestamps = 1
opcache.revalidate_freq     = 0
